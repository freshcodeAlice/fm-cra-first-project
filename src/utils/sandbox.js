/*
/// класовий setState

 prevState = {
    x: 0,
    count: 0
 }
 this.setState({
    count: count+1
 })


 {...prevState, ...newState} --> {x:0, count:1}


 ///// хук setState

 prevState = {
    x:0,
    count: 0
 }

 setState({
    count: count+1
 })

 {...newState}

 */